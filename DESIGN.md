# TETH AI Design System

## 1. 문서 역할

이 문서는 TETH 웹의 UI·UX 단일 기준이다. 화면 구조, 시각 언어, 대화 동작, 차트 표현, 반응형, 접근성, 카피와 상태 표현을 정의한다.

제품 범위와 퍼널은 `PRODUCT.md`, 제품·아키텍처·보안·출시 게이트는 `../tesia-program/PM/TESIA_AI_POC_Product_Implementation_Plan.md`, API와 데이터 의미는 `../tesia-contracts`가 우선한다. 이 문서는 그 계약을 바꾸지 않는다.

### 기준 레퍼런스

- 클라이언트 시각 정본: 배포본 `https://aresjoo.github.io/tesia-lab/`
- 소스 고정점: `aresjoo/tesia-lab` `main` commit `acccc7f802ac34333d666431b994717a0507e6c3`
- 그대로 이식할 것: 로고·파비콘, 첫 화면 문구 풀, 색상 토큰, 64px 접힌 레일, 우측 상단 인증 내비게이션, 별 필드, 중앙 컴포저, 빠른 선택, 모바일 앱 배너와 하단 도킹 구조, 팝오버와 전환 감각. **리서치를 포함한 대화 전체**의 작업 타임라인, `생각을 정리하는 중` 표시, 단계별 상세, 경과 시간, 문서 생성·수정·재검증 경험도 포함한다. 첫 화면과 대화 셸만을 이식 완료 범위로 해석하지 않는다.
- React로 대체할 것: 단일 HTML 구조와 직접 DOM 조작. 동일한 화면과 동작을 상태가 분리된 React 컴포넌트로 구현한다.
- 이식하지 않을 것: 브라우저 합성 백테스트, 하드코딩된 연구 타이머, 로컬 파일 저장, 터널 프록시, 클라이언트가 전달하는 시스템 프롬프트

`tesia-lab` 배포본은 TETH 프론트의 시각·카피·상호작용 정본이다. 임의 재해석, 다른 로고, 추가 후기·지표·마케팅 블록을 첫 화면에 섞지 않는다. 제품·API·보안 의미론은 PM과 계약 정본을 유지한다.

### 확정 통합 원칙과 PM 인계

사용자가 **원본 기반 React 프론트 재구성**을 확정했다. 기존 Mock 퍼널·질문 분기에 원본 모양을 끼워 넣는 방식을 중단한다. 원본의 화면·문구·SVG·문서 종류·노출 조건·상태 전환을 계승한 뒤 승인된 실제 대화/연구 데이터와 기존 전문 차트·결과 재생·거래소 연결을 붙인다. React는 내부 구현 변경이지 사용자 경험 재설계가 아니다.

PM 정본 `../tesia-program/PM/TESIA_AI_POC_Product_Implementation_Plan.md` §16의 **클라이언트 원본 계승과 실제 기능 통합**이 작업 순서와 팀 경계를 소유한다.
이전 포크의 PM 로컬 기록과 최신 PM의 수신 상태를 구분한다. 현재 통합은
`feat/web/ui-service-integration`, Web base `c156d10`, PM 소비 `a40ce9a`다.
기존 FE 후속 commit `5c79fa7`까지 Web #25에 인계했으며 PM 수신·상태 표 반영과 운영 승격은 별도 확인한다.
1단계 이식·회귀 결과는 [Bugfix_report.md](Bugfix_report.md#integration-baseline)를 따른다.

- 이 문서의 일반적인 미감 지침이나 기존 Mock 구현 설명이 원본과 다르면 원본 UI를 임의 변경하는 근거로 사용하지 않는다. 해당 차이를 이식 추적에 기록하고 원본에 맞춘다. 보안·권한·데이터 의미의 충돌은 PM/contracts와 해소한다.
- `연구 진행 중`, `생각을 정리하는 중`, Critic 발견·반박·수정·재검증 같은 제품 표현은 유지한다. 개발 상황을 설명하기 위해 본문을 Mock 안내, 고정 응답, 새 중간 페이지로 대체하지 않는다.
- 시각 검수용 샘플 출처·재생 조작부는 제품 본문 밖 개발용 경계에 격리한다. 모든 행·문서·답변에 개발 고지를 반복 삽입하지 않는다. 실제 서비스 오류/권한/위험/데이터 출처 안내를 숨기거나 샘플을 실제 검증처럼 표시하라는 의미는 아니다.
- 원본의 정적 문구·동선·미감을 바꾸려면 차이를 먼저 제시한다. 줄바꿈·겹침·포커스·접근성 결함은 원본 내용·동선을 유지하며 교정한다.
- 원본 화면 일치, 실제 API 연결, 결과/거래 의미, 성능과 전체 연속 사용은 따로 검증한다. 일부 컴포넌트의 테스트 통과로 전체 원본 계승 완료를 선언하지 않는다.

## 2. 제품 경험의 한 문장

사용자는 시장에 대해 편하게 묻다가 같은 대화 안에서 자신의 생각을 전략으로 만들고, 검증 결과와 차트를 보며 TETH와 계속 대화한다.

이 경험에서 대화는 입구가 아니라 제품의 중심이다. 원본의 대화 세션과 재진입 맥락을 보존한다. 원본 `tf*`는 검증 이후 전용 hash workspace를 의도적으로 사용하므로 이를 없애고 모든 화면을 대화 문서 안으로 강제하지 않는다. 반대로 원본의 `gDoc*` 연구 문서는 같은 대화 문서 탐색 흐름을 보존한다.

## 3. 핵심 원칙

1. **한 대화, 하나의 작업 공간**<br>
   시장 질문과 전략 생성은 탭으로 분리하지 않는다. 사용자의 의도가 바뀌어도 대화 맥락과 화면 정체성은 유지한다.

2. **대화가 명령 센터**<br>
   차트와 보고서는 대화에서 생성되는 아티팩트다. 아티팩트를 열어도 질문 입력과 현재 세션은 사라지지 않는다.

3. **결과는 시각화하고 판단은 설명한다**<br>
   수익률만 크게 보여주지 않는다. 최대 낙폭, 거래 수, 수수료, 표본 기간, 기준 자산과 함께 읽히게 한다.

4. **전문성은 밀도와 정렬에서 만든다**<br>
   네온, 과한 글로우, 유리 카드 남용으로 금융 전문성을 흉내 내지 않는다. 숫자 정렬, 차트 축, 상태의 정확성, 정보 위계로 신뢰를 만든다.

5. **Liquid Glass는 조작 층에만 사용한다**<br>
   내비게이션, 컴포저, 팝오버, 드로어, 떠 있는 툴바에 제한한다. 본문, 표, 차트 배경은 대비가 안정적인 불투명 면을 사용한다.

6. **표시된 진행은 실제 상태다**<br>
   사고 중, 데이터 수신, 백테스트 실행, 완료 표시는 실제 이벤트와 연결한다. 정해진 시간표로 진행을 연출하지 않는다.

7. **제품 경험과 개발 검수 경계를 분리한다**<br>
   원본 제품 레이아웃·상태·문구를 유지한다. fixture 검수는 본문 밖 개발 환경 표시로 구분하고 실제 API의 오류·권한·데이터 출처는 필요한 맥락에 표시한다. 반복 Mock 설명으로 연구 문서를 바꾸지 않는다.

## 4. 정보 구조

### 데스크톱

```text
┌──────────────┬──────────────────────────────┬──────────────────────┐
│ 세션 사이드바 │ 대화 워크스페이스             │ 아티팩트 패널          │
│ 64 / 256px   │ 본문 최대 820px              │ 목록 300px             │
│ 새 대화      │ 메시지, 사고, 스트리밍         │ 차트, 전략, 보고서      │
│ 대화 기록    │ 인플로우 하단 컴포저          │ 필요할 때만 열림        │
└──────────────┴──────────────────────────────┴──────────────────────┘
```

- 첫 진입에서는 사이드바를 접고 중앙 대화 영역을 넓게 쓴다.
- 로그인 후에는 세션 목록을 제공하되, 대화가 없는 상태에서는 시각적 소음을 만들지 않는다.
- 확장 메뉴의 순서는 `새 전략 → 연구 기록 → 예약된 검증 → 랭킹 → 전략 공유 → Research`다. 원본처럼 비로그인 확장 메뉴의 새 전략은 비활성이고, 축소 레일의 새 전략은 대화 초기화로 동작한다. 이 UI 차이를 실제 인증·주문 권한 검사로 사용하지 않는다.
- 메뉴 SVG는 원본 path/viewBox를 `ClientIcon`에서 공유한다. 기본 stroke 1.6, 패널 열기·닫기 1.7, 프로필 1.5와 각 원본 크기를 보존하며 장식 SVG는 접근성 트리에서 제외한다.
- 아티팩트 패널은 차트나 문서가 생성되는 순간 자연스럽게 열린다.
- 중앙은 flex 열, 우측 Artifacts 목록은 300px의 인플로우 패널이다. 1100px 이하에서는 우측 목록을 숨기고 헤더의 Research Plan 탭으로 동일 문서를 연다. 고정 오버레이 전략 카드를 사용하지 않는다.
- 전역 상단바를 추가해 세로 공간을 낭비하지 않는다. 세션 제목, 상태, 패널 토글은 대화 헤더 한 줄에 둔다.

### 모바일

- 기본 구조는 단일 대화 열이다.
- 세션 목록은 좌측 드로어, 아티팩트는 중앙 Research Plan 탭으로 연다. 하단 입력창은 항상 유지한다.
- 아티팩트에서 닫기를 누르면 같은 스크롤 위치의 대화로 돌아온다.
- 하단 컴포저는 `visualViewport`를 기준으로 키보드 위에 유지한다.
- 상시 하단 내비게이션과 컴포저를 동시에 두지 않는다.

## 5. 주요 화면과 상태

아래 표는 기존 Mock 구현의 요약이며 원본 상태 목록을 대체하지 않는다. 특히 전략 질문 분기·가입 UI·결과 이동은 원본과 대조해 재구성할 대상이다. 전체 원본의 문서·상태·전환 합격 기준은 §18의 이식 추적 및 원본 기반 재구성 순서를 따른다.

| 상태 | 중심 콘텐츠 | 유지되는 것 | 주 행동 |
|---|---|---|---|
| 홈 | 웅장하지만 절제된 히어로와 컴포저 | 브랜드, Mock 경계 | 질문 시작 |
| 자유 대화 | 사용자 질문과 TETH 답변 | 같은 컴포저, 같은 세션 | 이어서 묻기 |
| 전략 수렴 | 한 번에 한 가지 확인 질문 | 이전 대화 | 답변 또는 빠른 선택 |
| 전략 초안 | 대화 안 요약 카드, 우측 전략 문서 | 대화 입력 | 수정, 백테스트 |
| 가입 | 대화 위 모달 또는 시트 | 배경의 전략 맥락 | 간편 가입 |
| 백테스트 실행 | 대화와 실행 타임라인, 차트 아티팩트 | 중지·건너뛰기 | 진행 보기 |
| 결과 | 차트, 핵심 지표, TETH 해석 | 대화 입력 | 수정, 비교, 다음 단계 |
| 실패·경고 | 무엇이 약했는지와 수정안 | 원 결과 | AI 수정 후 재검증 |
| 실행 준비 | 승인 상태와 연결 환경 | 검증 결과 | Mock/Paper 준비 |

가입 시점은 `PRODUCT.md`를 따른다. 시장 질문과 전략 생성은 비로그인으로 열고, 백테스트 실행 직전에 가입을 요청한다.

## 6. 홈과 첫 질문

### 구성

1. 데스크톱 접힌 레일, 모바일 TETH 심볼
2. 클라이언트 정본 문구 풀에서 매 방문 하나의 히어로 문장
3. 해당 문구와 짝지어진 보조 문장
4. 중앙 컴포저
5. 세 개의 정본 시작 칩
6. 입력 시 노출되는 무료 배지와 하단 약관 한 줄

히어로와 컴포저가 첫 화면의 시선 중심이어야 한다. 메뉴, 가격, 후기, 시장 지표가 첫 질문보다 먼저 경쟁하지 않는다.

### 히어로 배경

- 캔버스는 아주 느린 별 필드와 중심부의 미세한 깊이만 표현한다.
- 입자는 장식이 아니라 시선이 컴포저로 모이도록 움직인다.
- 강한 물방울, 반복 파문, 번쩍임, 커서 추적 광원은 사용하지 않는다.
- 평균 프레임 예산은 60fps 환경에서 3ms 이하를 목표로 하고 DPR은 2로 제한한다.
- 탭이 숨겨지거나 캔버스가 화면 밖이면 애니메이션을 중지한다.
- `prefers-reduced-motion`에서는 정적인 한 프레임으로 대체한다.

### 컴포저

- 컨테이너 기본 높이 58px, textarea는 21px 줄높이로 최대 147px까지 자동 확장한다. 한 줄에는 스크롤바가 없고, 최대 높이를 넘긴 긴 입력은 스크롤할 수 있다.
- 데스크톱 최대 너비 640px
- 내부 좌우 여백 20px
- 입력이 생기기 전 전송 버튼을 과하게 강조하지 않는다.
- Enter 전송, Shift+Enter 줄바꿈, IME 조합 중 Enter 무시
- 빈 입력은 전송하지 않고 입력 아래에 짧은 오류를 표시한다.
- 플레이스홀더는 최신 원본 `phRotStart`를 따른다. 언어별 고정 접두사와 10개 예시를 70ms 타자·24틱 유지·2글자 삭제로 순환한다. 860px 이하 짧은 접두사, reduced motion 고정 첫 문구를 보존한다. 기존 고정 예시 한 문장은 최신 원본 계승 기준이 아니다.

## 7. 대화 동작

### 응답 시퀀스

```text
전송 → 사용자 메시지 고정 → 사고 표시 → 필요한 실제 도구 이벤트 → 첫 토큰 → 스트리밍 → 후속 행동
```

- 전송 즉시 사용자 메시지를 현재 뷰 상단 근처로 부드럽게 올리고 답변 공간을 확보한다.
- 250ms 안에 응답이 시작되지 않으면 TETH 사고 표시를 보여준다.
- 사고 표시는 원본 `g-act2`의 로고 헤더·경과 시간·접을 수 있는 단계별 상세로 구성한다. `생각을 정리하는 중` 헤더와 `생각하는 중` 행을 사용한다. 세 점 한 줄만으로 전체 리서치 경험을 대체하지 않는다.
- 데이터 조회, 지표 계산, 백테스트 단계는 실제 이벤트가 발생할 때만 접을 수 있는 작업 타임라인에 추가한다.
- 첫 토큰이 오면 사고 표시를 답변 헤더로 자연스럽게 전환한다.
- 스트리밍 중에는 문장 끝에 작은 캐럿만 사용한다. 블록 전체 shimmer는 사용하지 않는다.
- 중지 버튼은 생성 중일 때만 전송 버튼 자리를 대신한다.

### 스크롤

- 사용자가 하단 140px 안에 있을 때만 새 콘텐츠를 자동 추적한다.
- 사용자가 위 내용을 읽고 있으면 위치를 강제로 바꾸지 않고 `새 답변 보기` 칩을 표시한다.
- 응답 종료 후 불필요한 하단 빈 공간을 제거하되 스크롤 점프가 없어야 한다.
- 세션을 다시 열면 마지막 읽은 위치 또는 마지막 사용자 메시지 위치를 복구한다.

### 질문과 빠른 선택

- 빠른 선택은 키보드를 대체하지 않는다. 항상 직접 입력할 수 있다.
- 한 번에 한 질문만 한다.
- 선택지는 2–5개로 제한하고 각 선택지는 결과가 무엇인지 이해되는 문장으로 쓴다.
- 선택 후 버튼 묶음은 읽기 전용 선택 요약으로 바뀐다.
- 시장 질문에서 전략 생성으로 전환되어도 새 화면으로 이동하지 않는다.

## 8. 아티팩트

아티팩트는 대화 중 생성된 구조화 결과다. 원본 `gOpen`의 plan/hypo/strat1/strat2/bt1/bt2/critic/stress/holdout/report/connect/run/live와 대화 차트를 전부 추적한다. 임의의 5종 분류로 원본 문서를 축소하지 않는다.

### 공통 규칙

- 대화 안에는 1–3줄 요약과 `열기` 행동만 둔다.
- 상세 내용은 우측 패널 또는 모바일 전체 화면 시트에서 연다.
- 아티팩트 헤더에는 제목, 버전, 상태, 생성 시각을 표시한다.
- 원본처럼 Strategy v1/v2, Backtest v1/v2는 서로 다른 artifact ID다. 열린 문서 탭은 원본 `gTabAdd`의 최대 5개·첫 탭 보존 규칙을 따른다. 미구현 상태에서 버전들을 하나의 문서로 합치지 않는다.
- 대화에서 특정 수치를 클릭하면 아티팩트의 해당 위치를 강조한다.
- 아티팩트에서 질문하면 대화에 컨텍스트 칩을 붙인다. 예: `Backtest v2에 대해 질문`.

### 세션과 아티팩트 명칭

- 세션 제목은 사용자의 첫 의도를 2–6단어로 요약한다.
- 사용자는 `TETH`와 대화한다. 원본 Research Team 및 작업 단계의 역할 표시는 보존 대상이지만, 실제 호출된 작업만 표시한다. 호출하지 않은 다중 모델의 합의나 검수를 만들어 넣지 않는다.
- 세부 실행 주체가 필요한 감사 화면에서만 `전략 구조화`, `데이터 검증`, `리스크 점검`처럼 역할 중심으로 표시한다.

## 9. 전문 차트

### 차트 목적

차트는 장식이나 배경 애니메이션이 아니라 전략이 언제, 왜, 어떤 가격에서 행동했는지 검증하는 작업 도구다.

### 원본 연구 문서와 연결하는 현재 구현

사용자가 승인한 배치는 **대화 → Research Plan → Activity/Critic → Backtest/Final Report → 선택적 상세 차트**다. 홈·기본 대화·연구 타임라인과 원본 문구는 이 기능을 이유로 바꾸지 않는다.

- 완료된 Backtest v1/v2 및 Final Report의 `차트로 자세히 보기`에서 같은 세션의 확장 아티팩트를 연다. 독립 터미널·새 전역 메뉴·새 대화 세션을 만들지 않는다.
- 데스크톱은 왼쪽 차트/지표/거래 목록, 오른쪽 **원래 문서의 질문·답변·초안**을 사용한다. 860px 이하에서는 차트와 대화를 한 번에 한 면씩 표시한다. 선택한 거래는 질문의 참고 대상으로만 추가한다.
- 복귀 버튼과 브라우저 뒤로가기는 원래 문서·읽던 위치·질문 초안으로 돌아간다. 버전별 확대 범위·거래 선택·재생 여부는 해당 연구 화면의 메모리 상태다. 새로고침 후 확대/선택까지 보존한다고 보장하지 않는다.
- 첫 상세 진입에서만 기록된 결과를16초 동안 재생한다. Skip/Escape는 **시각적 재생만** 끝내며 연구 작업이나 서버 job을 취소·완료시키지 않는다. 감소 모션은 자동 재생을 생략한다. 이후 수동 거래 재생은 허용한다.
- 원본 `g0/g1/g2/g3/gt/gt2/gl/gb2/gg/gr`와 폰트·로고를 계승한다. 본문/차트/표는 불투명 바탕, 작은 체결 강조에만 글래스를 적용한다. 마커 라벨 간격을 확보하되 모든 매수·매도 화살표와 거래 행은 유지한다.
- 현 공개 Client fixture에는 불규칙 가격 표본, 봉 인덱스, Research 거래·자산곡선만 있다. `ClientResearchAnalysis`는 Lightweight Charts5.2.1의 선·계단형 자산곡선·거래손익 막대로 이를 그대로 표시한다. **OHLC·거래량·실체결가·수량·실제 분봉을 만들어 넣지 않는다.** 축의 내부 정렬 키는 timestamp 계약이 아니며 화면에는 원본 봉 번호만 표시한다. Holdout은 Research 가격/거래와 섞지 않는다.
- 문서와 상세 차트의 v1 27거래/v2 10거래 및 성과 수치는 동일 fixture를 공유한다. 정적 문서 답변 경로도 공유하며 실제 AI/실제730일 백테스트 연결로 표시하지 않는다.
- 차트 코드는 상세 진입 때만 지연 로드한다. 청크 실패는 문서 트리를 유지한 복귀 안내로 격리한다. 종료 시 차트/마커/이벤트/RAF를 해제한다. 재생은910개 슬롯에 한정해 최대 약12.5Hz로 그리며 숨긴 탭에서는 그리지 않고 실제 경과 시간으로 재개한다. 대규모 실데이터 성능 검증과는 별도다.

아래 기본 레이아웃은 승인된 **실데이터가 공급될 때의 목표 기준**이다. 현재 원본 fixture 확장만으로 기존 전문 작업본의 실제 OHLC/라이브·지표·drawing 전체 이식 또는 실제 서비스 완료를 선언하지 않는다. `web-lightweight-backtest-workspace`의 미커밋 전문 차트 작업과 내부 API 경로는 보존한다. 상세 검증·남은 범위는 `Bugfix_report.md`의 상세 차트 통합 절을 따른다.

### 기본 레이아웃

현 디자인 적용 준비물(제품 미연결): `ClientProfessionalPriceChart`는 같은 연구 토큰 안에서 OHLC·거래량·EMA·가격 눈금을 표시하는 독립 컴포넌트다. 입력 정밀도·UTC 체결 시각을 보존하고 동일 봉의 마커는 방향별 건수를 표시하며 개별 원장은 그대로 선택한다. 좁은 화면의 눈금/보조지표 버튼은 정렬된 그룹으로 줄바꿈한다. 그래프를 읽는 바탕은 불투명하게 유지하며 장식용 배경·새 로고를 추가하지 않는다.

독립 렌더러의 재생은 완료된 입력을60초에 걸쳐 증분 표시한다. 상단 overlay는 본문을 밀지 않고 Skip은 재생만 종료한다. 체결 강조는460ms의 transform/opacity 한 번으로 제한한다. API에 아직 연결되지 않았으므로 위 원본 fixture 상세의16초 동작과 구분한다. `ClientBacktestWorkspace`는 DEV 미리보기에서 전체 창 차트로 시작해 Skip/완료 시 같은 캔버스를 유지하면서 좌측 차트·원장/우측 대화로 전환한다. 전환은 시작/끝 실측1회씩과460ms transform/opacity만 사용하며 레이아웃을 매 프레임 계산하지 않는다. 860px 이하는 차트·대화 전환 버튼을 제공하고 초안은 부모가 보존한다. 실제 대화 ReactNode·질문/선택 체결 callback·입력 데이터는 호출자가 공급하며 이 컴포넌트는 서버 job 상태를 생성하거나 연장하지 않는다. 재진입 자동 재생 정책도 호출자가 `autoReplay`로 결정한다.

데이터가 없거나 입력이 손상되면 합성 봉으로 대체하지 않으며 대화를 계속 사용할 수 있다. 이 컴포넌트의 `PriceChartView`를 새 공용 계약으로 승격하지 않는다. 승인된 계약·producer 전까지 제품의 tfw-chart에 임의 실데이터를 붙이지 않는다. 합성 미리보기와 실제 source-bound 연결을 구분하고, 후자는 미완료다. 진행 상태와 증거는 [준비 검수](Bugfix_report.md#professional-renderer-preparation)를 따른다.

원장은50행 고정 페이지로 제공하고 차트에서 체결을 선택하면 해당 페이지로 이동한다. 총10000건이어도 원장 DOM을10000행으로 늘리지 않는다. 검색/방향 변경은 첫 페이지로 복귀한다. 대화는 실제 DOM 메시지 변경만 관찰하며 한 프레임으로 묶어 하단을 따라간다. 위쪽 기록을 읽고 있으면 위치를 보존하고 `새 메시지 보기`로 복귀한다. 숨김·반응형 높이 변경을 사용자의 위쪽 스크롤로 오인하지 않는다. 실제 요청 Promise가 대기 중일 때만 전송 상태를 표시하고, 실패하면 입력을 남긴다. 허구 AI 진행 설명·지연·메시지는 만들지 않는다. 화면 해제는 소유자의 서버 작업 취소가 아니며 새 HTTP 계약을 만들지 않는다.

- 상단: 자산, 타임프레임, 데이터 범위, 데이터 출처, `MOCK DATA` 또는 `실데이터` 상태
- 중앙: OHLC 캔들, 거래량, 진입·청산 마커, 현재 선택 거래
- 하단: 자산 곡선 또는 drawdown 패널
- 우측 또는 하단 상세: 거래 사유, 체결가, 수량, 수수료, 슬리피지, PnL
- 기간이 2년 이상이어도 전체 맥락을 볼 수 있는 overview와 확대 구간을 함께 제공한다.

### 표현 규칙

- 양봉과 이익은 녹색, 음봉과 손실은 적색을 사용하되 색상 외에 방향·부호·아이콘을 함께 표시한다.
- BUY는 캔들 아래 위쪽 화살표, SELL은 캔들 위 아래쪽 화살표로 표시한다.
- 마커가 밀집하면 클러스터링하고 확대할 때 개별 체결을 펼친다.
- 마커 선택 시 차트와 거래 목록을 양방향으로 동기화한다.
- 실행 애니메이션은 실제 결과의 시간 순서대로 진행한다. 속도 선택과 `건너뛰기`를 제공한다.
- 체결 순간의 강조는 420–620ms 한 번만 사용한다. 파동은 최대 2겹이며 수익률 배지는 실제 거래 PnL을 표시한다.
- 완료 후 전체 화면 차트가 작업 공간의 아티팩트 패널로 축소되는 전환은 FLIP 기반으로 420–520ms 안에 연결한다.
- 1분, 1시간, 2시간, 4시간 봉은 서로 다른 집계 결과와 축을 보여야 한다. 같은 데이터를 라벨만 바꾸지 않는다.

### 데이터 경계

- 차트는 계약으로 받은 timestamp, OHLCV, 주문, 체결, 수수료, PnL을 그대로 투영한다.
- 브라우저가 임의 가격이나 체결을 생성하지 않는다.
- 값이 없으면 0으로 채우지 않고 `데이터 없음` 또는 `계산 중` 상태를 보여준다.
- Mock 데이터도 실제 계약과 같은 스키마를 사용하되 화면에 `MOCK DATA`를 지속 표시한다.

## 10. 백테스트 결과

### 첫 번째 읽기 순서

1. 결과 판정 한 문장
2. 누적 수익률과 최대 낙폭
3. 승률과 총 거래 수
4. 기준 자산 대비 성과
5. 수수료·슬리피지·데이터 기간
6. 차트와 개별 거래
7. TETH의 수정 제안

숫자는 단독으로 성공이나 실패를 선언하지 않는다. 거래 수가 작으면 표본 부족을 같은 위계로 경고한다.

### 지표 형식

- 금융 숫자는 `font-variant-numeric: tabular-nums lining-nums`
- 양수에는 `+`, 음수에는 `−`, 0에는 부호를 붙이지 않는다.
- 퍼센트는 기본 소수점 한 자리, 가격은 자산 tick size를 따른다.
- 기간은 명시적 시작일과 종료일을 함께 제공한다.
- 모든 지표 툴팁은 정의, 계산 기준, 데이터 출처를 포함한다.

### 나쁜 결과

- 위협적인 문구 대신 가장 큰 실패 원인과 영향을 먼저 설명한다.
- `원안 유지`, `AI 수정`, `조건 직접 수정` 중 최대 3개 행동을 제공한다.
- AI 수정은 바뀌는 조건과 기대 효과를 적용 전에 보여준다.
- 재검증 후에는 이전 버전과 새 버전의 delta를 동일 축으로 비교한다.

## 11. 시각 언어

### 색상

| 토큰 | 기준 값 | 용도 |
|---|---:|---|
| `--canvas` | `#0f1012` | 전체 배경 |
| `--surface-1` | `#17181b` | 레일, 본문 면 |
| `--surface-2` | `#1d1f23` | 보조 면, 입력 |
| `--surface-3` | `#26282c` | hover, 선택 |
| `--line` | `rgba(255,255,255,.10)` | 기본 경계 |
| `--line-strong` | `rgba(255,255,255,.18)` | focus, 선택 경계 |
| `--text-1` | `#e6e7ea` | 주요 텍스트 |
| `--text-2` | `#a6aab2` | 설명 |
| `--text-3` | `#6f747d` | 장식·비활성, 필수 정보는 text-2 |
| `--accent` | `#3d6ef0` | 주요 행동, 활성 상태 |
| `--positive` | `#60c88c` | 이익, 상승 |
| `--negative` | `#eb786e` | 손실, 하락 |
| `--warning` | `#d9a45b` | 주의, 검토 필요 |

- 브랜드 심볼의 청록·청색·황색은 로고와 아주 작은 브랜드 광원에만 사용한다.
- 자주색 AI 그라데이션은 사용하지 않는다.
- 페이지마다 색 체계를 바꾸지 않는다.

### 타이포그래피

- UI: `Noto Sans KR Variable`, 시스템 산세리프 폴백
- 숫자: `Geist Variable`, tabular-nums
- 본문 기본 15px / 1.65
- 대화 본문 15–16px, 최대 행 길이 68ch
- 히어로 데스크톱 clamp(30px, 3.8vw, 44px), 모바일 30px, weight 500, line-height 1.28
- 화면 제목 20–24px
- 보조 정보는 최소 12px
- 대문자 영문 라벨은 최소화하고 letter-spacing은 0.08em 이하로 한다.

### 간격과 형태

- 기본 간격 단위: 4px
- 주요 스케일: 4, 8, 12, 16, 20, 24, 32, 40, 56, 72
- 콘텐츠 패널 radius: 12–16px
- 떠 있는 컴포저와 팝오버 radius: 20–28px
- pill은 태그, 상태, 짧은 선택에만 사용한다.
- 모든 섹션을 카드로 감싸지 않는다. 배경 면, 1px 구분선, 여백을 먼저 사용한다.

### Liquid Glass

```css
background: color-mix(in srgb, #1c1f24 76%, transparent);
border: 1px solid rgba(255,255,255,.12);
box-shadow: 0 16px 48px rgba(0,0,0,.28), inset 0 1px rgba(255,255,255,.05);
backdrop-filter: blur(24px) saturate(125%);
```

- 같은 화면에서 강한 glass surface는 최대 2개만 둔다.
- 텍스트 뒤 투명도는 명암 대비 4.5:1을 해치지 않아야 한다.
- `prefers-reduced-transparency` 또는 저사양 환경에서는 `--surface-2` 불투명 면으로 대체한다.

## 12. 모션

| 범주 | 시간 | easing | 용도 |
|---|---:|---|---|
| 즉시 피드백 | 120–180ms | ease-out | hover, press, focus |
| 요소 등장 | 220–320ms | `cubic-bezier(.16,1,.3,1)` | 메시지, 팝오버 |
| 패널 전환 | 320–480ms | `cubic-bezier(.16,1,.3,1)` | 사이드바, 아티팩트 |
| 차트 전환 | 420–620ms | ease-out | 확대, 전체 화면 축소 |

- 동시에 움직이는 중심 요소는 하나만 둔다.
- 반복 pulse는 실행 중 상태와 현재가 갱신에만 사용한다.
- 완료 애니메이션은 한 번만 재생한다.
- 모션이 정보 이해를 늦추면 제거한다.
- reduced motion에서는 이동을 opacity 전환으로 대체하고 자동 재생 차트는 즉시 완료 상태로 제공한다.

## 13. 상태와 피드백

### 공통 상태

- 빈 상태: 다음 행동 하나와 예시 하나
- 로딩: 무엇을 기다리는지 구체적으로 표시
- 부분 성공: 성공한 결과를 보존하고 실패한 부분만 재시도
- 오류: 원인, 영향, 다시 시도, 안전한 다음 행동
- 오프라인: 기존 대화를 읽을 수 있게 유지하고 새 실행을 막음
- stale 데이터: 마지막 갱신 시각과 새로고침 행동 제공
- 권한 없음: 숨기지 말고 필요한 권한과 현재 가능한 행동 설명

`성공`, `위험`, `연결됨`을 색 점 하나로만 표현하지 않는다. 아이콘과 텍스트를 함께 쓴다.

## 14. 카피

- 한국어로 먼저 쓰고 필요한 금융 용어는 바로 쉬운 말로 설명한다.
- 첫 문장에 결론을 둔다.
- 사용자가 말하지 않은 확신, 수익 약속, 긴급성을 만들지 않는다.
- `AI가 알아서`, `대박`, `계좌가 터진다` 같은 과장 표현을 제품 카피로 사용하지 않는다.
- 쉼표, 따옴표, 가운뎃점, 긴 대시로 리듬을 인위적으로 만들지 않는다.
- 버튼은 행동과 결과를 함께 쓴다. 예: `수정안 적용 후 재검증`.
- 같은 상태 이름은 모든 화면에서 동일하게 쓴다.
- 데이터 레이블은 `MOCK DATA`와 `실데이터`, 실행 레이블은 `PAPER`와 `BINANCE DEMO`로 고정한다. 공개 화면에서 Production 실행을 암시하지 않는다.

## 15. 반응형 기준

| 너비 | 동작 |
|---|---|
| 1280px 이상 | 접을 수 있는 세션 사이드바 + 대화 + 아티팩트 |
| 861–1279px | 축소 사이드바 + 대화 + 오버레이 아티팩트 |
| 641–860px | 단일 대화 + 드로어·전체 화면 아티팩트 |
| 320–640px | 모바일 컴포저, 16px 좌우 여백, 한 열 |

- 320px에서 가로 스크롤이 없어야 한다.
- 모바일 주요 선택 칩은 최소 44px 높이다. 클라이언트 정본의 압축형 대화 보조 버튼은 24px 이상을 유지한다(원본 메시지 액션 26px, 전송 30px). 이 예외로 다른 페이지의 44px 검증을 제외하지 않는다.
- 후기와 긴 카피는 의미 구절 단위로 줄바꿈하되 화면보다 긴 구절에는 `nowrap`을 강제하지 않는다.
- 표는 중요 열을 먼저 유지하고 나머지는 가로 스크롤 또는 상세 시트로 보낸다.
- 차트 툴팁은 화면 가장자리를 넘지 않으며 모바일에서는 하단 정보 패널로 바뀐다.

### 전 화면 가독성 규칙

- 한국어 제목·문단·레이블은 `word-break: keep-all`로 어절을 우선 보존하고, 화면보다 긴 주소·전략명에는 `overflow-wrap: anywhere`로 안전한 줄바꿈을 허용한다. 전체 본문에 `nowrap`이나 임의 `<br>`를 추가하지 않는다.
- 짧은 제목은 `text-wrap: balance`, 일반 설명은 `text-wrap: pretty`로 마지막 줄의 고립을 줄인다. 사용자 입력의 실제 줄바꿈은 유지한다. 이 CSS를 지원하지 않는 브라우저도 기본 줄바꿈으로 내용을 읽을 수 있어야 한다.
- 금융 금액·지표는 숫자와 단위를 한 덩어리로 읽고, 월 단위 등의 보조 레이블은 별도 작은 요소로 둔다. 요금 카피·수치·Mock 경계를 바꾸는 조치가 아니다.
- 대화 제목만 공간에 따라 말줄임을 허용한다. 좁은 헤더의 제목 수정 중에는 문서 탭을 잠시 감추고 16px 입력창이 가용 너비를 사용한다. 수정 완료·취소 후 동일 탭을 복구한다. 본문과 전략 조건은 말줄임으로 숨기지 않는다.
- 대화 입력창은 내용·너비·폰트 로드에 맞춰 높이를 재계산한다. 새 응답 버튼은 실제 컴포저 높이 위에 놓고, 복사 오류는 메시지 아래 인플로우 상태로 표시한다. 매 프레임 측정하지 않고 `ResizeObserver`의 변경만 한 프레임에 합친다.
- 모바일 홈은 기본 하단 도킹을 유지하되 약관의 실제 높이에 맞춰 입력창과 칩 위치를 조정한다. 큰 글자/문단 간격 또는 짧은 화면에서 내용이 겹치면 React 상태로 인플로우 레이아웃으로 전환해 페이지 스크롤로 모두 접근할 수 있게 한다.
- 모바일 안내 페이지의 도움말은 페이지 하단에 둬 본문·면책 문구를 가리지 않는다. 도움말·설정 팝업은 짧은 화면에서 내부 스크롤로 닫기와 링크에 접근할 수 있어야 한다.
- 정책 개요는 고정 459px 두 열 대신 축소 가능한 그리드를 사용한다. 좁은 화면에서는 아이콘과 본문을 세로로 배치해 긴 설명의 읽기 폭을 확보한다. 정보 페이지 제목과 설명은 별도 행간을 사용하고, 701–1100px 제품 소개는 유연한 두 열로 과도하게 큰 이미지와 빈 여백을 줄인다.
- 모바일 결과 해설은 아이콘 옆 좁은 열에 본문을 몰지 않고 한 열로 제공한다. 대시보드 질문 레이블과 Mock 배지는 서로 다른 행을 사용할 수 있다.

## 16. 접근성

- WCAG 2.2 AA를 목표로 한다.
- 모든 기능은 키보드로 사용할 수 있어야 한다.
- focus ring은 배경과 3:1 이상 대비되고 잘리지 않아야 한다.
- 대화 스트리밍 전체를 매 토큰 읽지 않는다. 시작과 완료만 `aria-live`로 알린다.
- 차트에는 기간, 최종 수익, 최대 낙폭, 거래 수를 포함한 텍스트 요약과 거래 표를 제공한다.
- dialog와 drawer는 focus trap, Escape 닫기, 호출 요소 focus 복귀를 지원한다.
- 캔버스와 장식 SVG는 접근성 트리에서 제외한다.
- 아이콘 전용 버튼은 명확한 이름을 가진다.
- 색각에 관계없이 상승·하락·선택 상태를 구분할 수 있어야 한다.

## 17. 성능 기준

- 첫 화면 JS는 대화 시작에 필요한 코드만 적재한다.
- 차트, 보고서, 연결 UI는 필요할 때 지연 로드한다.
- 이미지와 로고를 거대한 base64로 HTML에 삽입하지 않는다.
- 캔버스는 DPR 2 제한, resize debounce, offscreen pause를 적용한다.
- 시세 업데이트는 전체 React 트리를 다시 렌더하지 않는다.
- 2년 이상의 1분봉 원본 전체를 DOM이나 React state에 복제하지 않는다. viewport 데이터와 집계 데이터를 분리한다.
- 목표: LCP 2.5초 이하, INP 200ms 이하, CLS 0.1 이하를 실제 배포 환경에서 측정한다.

## 18. 구현 경계

### 현재 React 정본 진입과 소유권

기본 진입점은 `ClientMainExperience`다. 클라이언트 `aresjoo/tesia-lab@acccc7f802ac34333d666431b994717a0507e6c3`의 홈·설정·계정·대화·연구·위임·공개 페이지를 연결한다. 기존 `TesiaApp`은 기본 UI가 아니며 보존 회귀용 DEV-only `?legacy-fixture=1`에서만 사용한다. production 빌드에서 이 쿼리는 활성화되지 않는다. 전문 차트 worktree의 미커밋 작업은 손대지 않았다.

- `ClientMainExperience/client-experience-store`: 세션, 원본 fallback 대화, 완료 기록, 이름·삭제·초안·화면 연결.
- `ClientAccountUI/ClientLocalePanel`: 설정 계층, 인증 단계, 공개 프로필, 피드백, 7언어·37통화.
- `ClientResearchWorkspace/client-research-fixtures`: Plan·Hypothesis·버전·Backtest·Critic·Stress·Holdout·Report·Connect·Run·Live와 Activity.
- `ClientDelegationWorkspace/client-delegation-fixtures`: 5문항·계약서·검증·수정·보고서·무료/유료 연결 UI·내 트레이딩.
- `ClientPublicPages/DownloadPreview/client-public-copy.json`: 정보·다운로드·정책, 원본 PI/PPL 번역·가격·기기 캡션·도움말.

### 데이터·기능·보안 경계

현재 4174는 로컬 시각 검수 환경이다. 원본 fallback 문장과 명시적 고정 연구/위임 사례를 재생하며 실제 AI·백테스트·인증·메일·피드백·결제·연결·주문은 처리하지 않는다. 환경 설명은 제품 문서 바깥의 공통 footer/details 한 곳에서 유지한다. 연구 재생 조작부는 일반 화면에 노출하지 않으며 개발 서버에서 명시적으로 `?ui-debug=1`을 선택한 검수에만 제공한다. production build에는 이 query로도 조작부가 나타나지 않는다. 모델 내부 사고 원문이 아니라 승인된 공개 작업 요약을 공급하는 표시 구조다. 실제 작업 실패를 고정 성공으로 덮어쓰지 않는다.

대화·연구 진행은 화면 가시성과 분리한다. 화면 이동·탭 숨김·pagehide·새로고침은 중지 요청이 아니다. 고정 연구 사례는 시작 시각과 경과 시간으로 동일 artifact/버전을 복원하고, 세션별 메모리 상태를 유지해 문서 unmount에 종속되지 않는다. 연구 화면 밖의 기록 상태도 완료 시 갱신한다. 숨김/비마운트 화면의 렌더 타이머는 정리하고, 관찰 시 현재 시점으로 따라잡는다. 연구만 실행 중일 때는 1초 간격, 답변 스트리밍 시에만 65ms 갱신한다. 사용자의 명시적 응답 중지와 삭제는 유지한다. 옛 자동 일시정지 캐시는 남은 분량부터 자동 복구한다. 위임 검증도 시작 시각으로 단계를 복원하지만 인증·결제·주문 권한을 배경에서 임의로 완료시키지는 않는다.

이는 로컬 fixture의 UI 연속성 규칙이다. 실제 연구는 서버 job/event가 권위 있는 상태이며 브라우저의 경과 시간으로 결과·Critic·완료를 생성하면 안 된다. 새 탭·다른 기기 간 동기화, 탭을 닫아 sessionStorage가 소멸한 뒤 복구, 실제 AI/서버 job 연속 실행은 이번 변경으로 연결되지 않았다. 저장 차단 시 동일 탭 연구 화면 왕복은 메모리로 유지하나 새로고침 보존은 보장하지 않는다.

대화에서 선택한 자산·방식·주기·손절·익절은 Plan/Hypothesis에 전달한다. 연구 결과는 고정 BTC/1일/SL−3%/TP+8% 사례이고 이를 ETH 등 선택 조건의 계산 결과로 재라벨링하지 않는다. 계획 수정은 메모에 반영되며 현재 결과 재계산 계약이 아니다. 위임의 74→86과 캔들도 UI fixture다.

비민감 대화·초안·문서·계획·공개 프로필은 탭 sessionStorage에 보존한다. API key/OTP/password/card/UID는 브라우저 저장소·로그·네트워크로 보내지 않는다. 위임 connect 재로드는 plan으로 돌아가며 자격증명 입력 상태나 연결 권한을 복원하지 않는다. 세션 삭제는 두 번 확인 후 해당 ID의 문서·재생·위임 캐시만 정리한다. 저장 실패는 입력을 막지 않고 경고·복구를 제공한다.

원본 첫 전송 인증과 PM의 비로그인 대화 정책 차이는 미결정이다. UI 작업으로 실제 인증 시점을 변경하지 않는다. 실제 어댑터는 승인된 `tesia-contracts` generated client와 서비스 계약을 소비한다. 대화/연구/공개 진행 이벤트/실제 결과 차트 통합은 별도 미완료다. 참고 구현은 `../../worktrees/web-owner-local-service-vertical/src/internal-poc/InternalPocApp.tsx`, 전문 결과 재생 보존 위치는 `../../worktrees/web-lightweight-backtest-workspace`다. 내부 POC 디자인으로 제품 UI를 대체하지 않는다.

### 원본 60항목 이식 추적

공개 페이지와 Git HTML은 676,303 bytes, SHA256 `61676041d40d403453d41a61b2be3428ef4bc5937fdd5442ebcab483dd0689f2`까지 같았다. 미공유 문제가 아니라 기존 퍼널의 부분 이관 문제였다. 아래 원본 줄 번호는 해당 commit 기준이다. **UI 복원은 실서비스·픽셀 완전 일치·실기기 검증 완료를 뜻하지 않는다.**

| ID | 원본 / 줄 | 원본 행동 | 현재 구현·미연결 경계 |
|---|---|---|---|
| A01 | gNew 4187 | 첫 전송 인증 gate·입력 보존·독립 세션 | UI 복원: 독립 세션/입력 보존. 첫 전송 인증 시점은 PM 결정 대기, 비로그인 대화 유지. |
| A02 | gNew 4203, taiMarket 6692 | 실제 AI 우선 자유 대화, 연결 불가 때 script fallback | 셸 교체: ClientMainExperience 기본 진입. 원본 gConv fallback만 연결; 실제 자유 대화 어댑터 미연결. |
| A03 | gSideRender 3921, gSelect 4172, taiAI 4841 | 세션별 메시지·차트·작업 컨텍스트 | UI 복원: 구조화된 세션/초안/작업 기록, 문서별 컨텍스트. 실제 차트·서버 세션 동기화 미연결. |
| A04 | gTitleEdit 3971, taiMarket 6853 | 자동 제목·수동 이름·목록/헤더 동기화 | UI 복원: 자동 제목/수동 이름/헤더·목록 동기화, 수동 이름 덮어쓰기 방지. |
| A05 | gSessMenu 4791, rename 4806, delete 4824 | 세션 이름 변경·삭제 메뉴 | UI 복원: ⋯ 이름 변경/2회 삭제 확인/정확한 세션 캐시 정리. |
| A06 | gConvUser 4257, taiUEdit/UCopy 4263 | 사용자 메시지 편집→입력 복원·복사 | UI 복원: 원문 입력 복귀·포커스·복사/실패. |
| A07 | gUserPin 4232, gScrollBottom 4004, taiSpacerTrim 4246 | 질문 리프트·답변 공간·수동 스크롤 존중 | UI 복원: 기존 g-scroll 질문 리프트·읽던 위치 존중. 실기기 키보드는 미검증. |
| A08 | taiStream 4924, taiMarket 6820대, taiMd 5103 | delta·throttle·증분 Markdown·내부 태그 숨김 | 부분: 로컬 스트리밍/중지/HTML 안전 텍스트. 실제 delta·Markdown·도구 태그 어댑터 미연결. |
| A09 | taiStop 4974, retry 4990 | 중지·재시도·계속하기 | 부분: 턴 중지/초안/부분 답변 보존, 재로드 중단 처리. 실제 네트워크 재시도 미연결. |
| A10 | actStart/Step/Think 5250/5296/5318 | 단계별 진행·흘러들기·상세 펼침 | UI 복원: 단계/상세/경과 시간. 공개 실제 진행 이벤트 미연결. |
| A11 | actFinish/RenderFinal/Hydrate 5419/5430/5457 | 완료 후 접힌 기록·다시 펼침·복원 | UI 복원: 완료/중단 접힌 기록·재펼침·새로고침 보존. |
| A12 | taiActs 5479, taiVote 5496, taiFeedback 5502 | 응답 복사·평가·의견 | UI 복원: 응답별 좋아요/싫어요/복사, 설정 의견 이미지. 실제 제출 미연결. |
| A13 | taiNext 5524, taiGauge 5549, taiAskCard 6405 | 후속 행동·게이지·조건 질문 왕복 | 부분: 원본 조건 선택/위임 5문항/이전·재시작. 실제 응답 ACT/ASK/gauge 연결 미완료. |
| A14 | taiChartOpen/Render 6473/6480, taiOhlc 6485 | 같은 대화의 전문 차트·시간봉·컨텍스트 | 미통합: 보존된 전문 차트 worktree와 실제 대화 OHLC 연결은 후속 범위. |
| A15 | gMakePlan 4356, gDocPlan 4550 | 계획 자동 생성·review 상태·8개 행 | UI 복원: 대화 조건 완성 후 8행 Plan, 자산/방식/주기/손절/익절 전달. |
| A16 | gRowComment 4566 | 행 아래 인라인 수정/적용 | UI 복원: 행별 인라인 수정 요청/적용/취소. 변경은 계획 메모이며 결과 재계산 아님. |
| A17 | gStartRunGo 4416, gShowActivity 4384 | 세션 연구·Activity·7역할 | UI 복원: 95초 fixture Activity/7역할/진행상태. 실제 연구 job 미연결. |
| A18 | gArtRender/Add 3942/3951, gTabAdd 3988 | 점진 artifact·상태·열린 탭 최대 5개 | UI 복원: 순차 artifact/13문서+Activity/최대5탭/첫탭·키보드·초안·스크롤. |
| A19 | gDocHypo 4585, gDocStrategy 4592 | 가설·쉽게 설명·Strategy v1/v2 | UI 복원: Hypothesis/Strategy v1·v2 원문, 생성시점 전 결과 비노출. |
| A20 | gDocBacktest 4601 | 버전 5통계·매매/봉인구간·곡선·연도 표 | UI 복원: 원본 고정 case 통계/BUY·SELL/봉인/자산곡선/연도표. 실제 run 미연결. |
| A21 | gDocCritic 4618, finding 4451대 | Activity 5행→별도 Builder/Critic/Verdict | UI 복원: Activity 5행과 Critic 3블록 분리, v1/v2 근거. |
| A22 | gDocStress 4626, gDocHoldout 4635 | 불리한 조건·판정·봉인구간·성능저하 | UI 복원: Stress 8종/Holdout, 고정 사례 판정·경고. |
| A23 | gFinishRun 4501, gDocReport 4647, gWhatIf 4674 | 완료→보고서·근거·Integrity·What-if | UI 복원: 95초 완료→Final Report/Integrity/근거·What-if. |
| A24 | gOpen 4520, gDocThread 4683, gSend 6996 | 문서별 대화·수정→새 버전 | UI 복원: 문서별 질문/초안/스크롤/코멘트. 실제 수정 버전 생성 미연결. |
| A25 | gDocConnect/Run/Live 4691/4705/4723 | 권한표·실행확인·Paper/Live·Reality Check | UI 복원: 연결 권한표/실행 확인/Paper·Live/중지·재개·종료. 실제 자금 실행 차단. |
| B01 | gSideToggle 7905, HTML 2190 | 레일·확장·모바일 스크림 | UI 복원: ClientChrome 원본 레일/확장/모바일/inert/포커스. |
| B02 | authSyncUI 7824 | 인증 상태별 상단/설정/다운로드 노출 | UI 복원: 인증 상태별 내비/지원/다운로드/프로필, 공개 이름 표시. |
| B03 | tethBannerClose 7800 | 배너 닫힘을 탭 세션 동안 보존 | UI 복원: 배너 닫힘 sessionStorage 보존. |
| B04 | gSetMenu/gmSub/gmFlip 7677–7735 | 설정 계층 팝오버/모바일 시트 | UI 복원: ClientAccountUI 설정 계층/팝오버/모바일 서브시트. |
| B05 | GLC_LANGS/glcRender/glcFilter 7263–7368 | 7언어·검색·체크·설명·모바일 탭 | UI 복원: 7언어/검색/체크/툴팁/560px·860px 시트. |
| B06 | glcSetLang/applyLang 7369/7562 | 저장·홈/내비/인증/피드백/ARIA 번역 | UI 복원: 원본 사전 기반 홈/내비/인증/피드백/공개 페이지. 사전 없는 원문은 임의 번역하지 않음. |
| B07 | GLC_CURS/fmtMoney/applyCurrency 7272/7667 | 37통화·검색·저장·지정 USD 표시 환산 | UI 복원: 37통화 선택/정적 표시 함수. 실제 환율/주문 통화는 별도 계약. |
| B08 | authOpen/Show/I18n 7120–7161 | Google/Apple/이메일·로그인/가입 | UI 복원: Google/Apple/이메일/로그인·가입/취소/SVG. 로컬 fixture, 실제 인증 미연결. |
| B09 | authEmailNext/SendCode/CodeNext 7164–7209 | 이메일·6자리 코드·재전송·비밀번호 전환 | UI 복원: 이메일/6자리 코드/재전송 대기/비밀번호 왕복/유효성. |
| B10 | authPwReq/NewNext/Next 7210–7246 | 비밀번호 조건·보기·코드 대체 | UI 복원: 비밀번호 조건/표시/코드 대체, secret storage·네트워크 없음. |
| B11 | authAgeShow/Finish 7248–7260 | 이름·연령·가입 로딩 | UI 복원: 이름/연령/로딩/오류. 원본 범위는 실제 법률 연령 게이트가 아님. |
| B12 | gProfileClick/gLogout 7836–7855 | 계정·이메일·로그아웃 | UI 복원: 이름/메일/로그아웃. 로컬 공개 프로필만 탭 보존. |
| B13 | fbOpen/Attach/Submit 7756–7790 | 의견·이미지·미리보기·완료 | UI 복원: 의견/10MB·형식/이미지 미리보기·제거/객체URL 정리. 실제 전송 없음. |
| B14 | gm-sub-help HTML 2302 | 설정→도움말→약관/개인정보/정보 | UI 복원: 설정 도움말→약관/개인정보/정보 내부 링크. |
| B15 | help-widget.js 1–90 | 원본 FAB SVG·팝오버·다국어·조건부 지원 | UI 복원: 원본 지원 카피/다국어 SiteHelp/키보드/모달 focus. |
| B16 | gSideRender 3921–3931 | Research/Live 목록·진행 상태 점 | UI 복원: 세션별 초안/진행/검토/Paper 상태; 실제 Live 서버 상태 미연결. |
| B17 | gSessMenu/RenameGo 4791–4822 | 헤더 메뉴·전략/세션 이름 동기화 | UI 복원: 헤더 메뉴/이름 편집/목록 동기화/취소/긴 이름. |
| B18 | gSessDelete 4824–4836 | 확인 후 해당 세션 삭제·작업 정리 | UI 복원: 2회 확인·현재 작업 제거·정확한 세션별 문서/연구/위임 캐시 정리. |
| B19 | gHistory/gStatusLabel 4757–4774 | 상태별 연구 테이블·복귀 | UI 복원: 기록 목록/빈 상태/진행상태/선택한 세션 복귀. |
| B20 | gSchedule 4775–4789 | 실행 중 전략 예약 행·빈 상태 | UI 복원: 빈 상태/실행 fixture별 주간 검증 행. 실제 예약 없음. |
| B21 | tfRankSeeds/View/Follow 6283–6329 | 원본 랭킹·내 공유 전략·따라하기 검증 | 부분: 원본3개 랭킹/로컬 공유 행/따라하기 초안. 실제 카피 검증 미연결. |
| B22 | tfShareView/Toggle 6330–6354 | 점수·수익·팔로워·보상·랭킹 반영 | 부분: 공유 스위치/복원/로컬 랭킹 반영. 실제 공개·보상 미연결. |
| B23 | tfSideSync/tfDashView 6240–6277 | 연결 후 내 트레이딩·중지/재개·체결 | UI 복원: 위임 완료 후 내 트레이딩/중지·재개·체결 fixture. 실제 제어 미연결. |
| B24 | about 283–400, download 219–277 | 각 페이지 별도 7언어 사전·저장언어 복원 | UI 복원: 별도 PI/PPL 7언어/가격/링크/55문구/다운로드 캡션. |
| B25 | download 286–316, policies route 368 | 3장 순환·스토어별 QR·정책 탭/앵커 | UI 복원: 3장/기기 비율/캡션/정책 탭·앵커. 실제 앱/QR 미연결. |
| C01 | taiActRun 5569, tfStart 5601, tfIntakeNext/Pick 5628/5645 | 위임 요청→자산·성향·예산·기간·허용하락 5문항, 완료 행 축약 | UI 복원: 원본5문항/완료행/재시작/자산·성향·예산·기간·손실 한도. |
| C02 | tfIntakeDone/Redo 5665/5688, tfVerifyGo 5694 | 전략 계약서·가정·다시 답하기·검증 후 이어서 보기 | UI 복원: 계약서/가정/다시 답하기/검증/세션 UI 복원. |
| C03 | tfNav/Route 6355/6356, tfResume 5705, tfPageMode 5731 | strategy/backtest/report/connect/done hash workspace, composer/aux 숨김 | 부분: React 내부 workspace·선택세션 복원. 원본 모든 hash 직진입 매핑은 후속. |
| C04 | tfWorkView/Run 5739/5769 | 좌측 TradingView·우측 조건/진행5행·차트 미지원 대체 | UI 복원: 좌측 fixture 캔들·우측 조건/5행, 1D/1W/1M 집계. 실제 TradingView 어댑터 미연결. |
| C05 | tfWorkResult 5794, tfScore 5863 | 점수·판정·지표·pass/maxTries 분기 | UI 복원: 점수 카운트/74→86 사례/판정/지표/진행. 실제 계산·실행 gate 아님. |
| C06 | tfRecommend/RetryRec/EditGo 5837/5846/5856, tfRankGo 5861 | 실패 조정안·재검증·직접 수정·추천 | UI 복원: 조정안/재검증/직접 수정/랭킹 연결, fixture 결과. |
| C07 | tfReportView/EzToggle/Markers 5887/5916/5927 | 리포트·매매 마커·금액/낙폭/승률/거래·쉬운말/전문가 | UI 복원: 보고서/거래 마커/지표/쉬운말·전문가 토글/반응형. |
| C08 | tfConnectView/PlanHtml/PlanPick 5982/6003/6015, tfFreeHtml 6095, tfCnUid 6136 | 4단계 progress·무료/유료·거래소/가입/UID/가이드 | UI 복원:4단계/무료·유료/거래소·UID·가이드/오류·재시도. 외부 가입 없음. |
| C09 | tfPayHtml/CardNext/Confirm 6022/6073/6083, tfApiHtml/CnApi 6157/6175 | checkout plan/payment/confirm/complete·API 권한/오류 | UI 복원: checkout/유효성/거절·재시도/API 권한·오류. 민감정보 비저장·미전송. |
| C10 | tfDoneView/Later/StartStrategy 6193/6217/6224, tfSideSync/DashView/StratToggle 6240/6242/6277 | 지금/나중에 시작·내 트레이딩·Running/중지·현황/체결 | UI 복원: 지금/나중/내 트레이딩/중지·재개/체결. 실제 제어 미연결. |

### 원본을 보존하며 보완한 가독성·상호작용

원본 860px 중단점, dark-only, SVG·문구·색상·300px Artifacts를 보존한다. 한국어/중국어 glyph 폴백, 긴 프랑스어 가격의 숫자그룹·너비별 크기, 320px 문단/차트 축, 모바일 메뉴와 뒤로 버튼 간격, 모달 focus/inert/Escape, 최대5탭의 키보드·선택탭 가시성, 약관/개발footer 및 저장경고/입력 가림을 보완했다. 홈 예시 순환은 DOM placeholder만 갱신하며 숨은 탭·입력 중·모션 최소화 시 불필요한 타이머를 정리한다.

원본 `gDoc`와 `tf`는 서로 다른 유효 흐름이다. 대화 조건이 완성되면 Research Plan, 위임 액션은 별도5문항 흐름으로 연결한다. 모든 페이지를 하나의 구형 result로 통합하지 않는다. 원본 모든 hash 딥링크/실제 Markdown·도구 스트림·차트·실서비스 전수 검증은 후속 범위로 남긴다.

## 19. 디자인 QA 합격 기준

### 대화

- 시장 질문과 전략 아이디어가 같은 입력에서 시작된다.
- 첫 질문, 후속 질문, 전략 수렴이 같은 대화 DOM과 세션 안에서 이어진다.
- 사고, 스트리밍, 완료 순서가 실제 상태와 일치한다.
- 응답 중 중지, 실패 후 재시도, 세션 전환이 기존 내용을 손상시키지 않는다.
- 저장된 세션 일부가 손상돼도 정상 세션과 홈 초안을 복원하고 부분 복원 안내를 유지한다. 자동 제목은 확정된 조건을 따르며 사용자 지정 제목은 덮어쓰지 않는다.

### 아티팩트와 차트

- 패널 개폐 후에도 대화와 차트 상태가 유지된다.
- 타임프레임 변경이 실제 다른 집계 데이터로 반영된다.
- BUY/SELL 마커, 거래 목록, PnL 상세가 동일 거래를 가리킨다.
- Mock 값과 실제 값을 한 화면에서 혼동할 수 없다.
- 백테스트 비교는 동일 기간·동일 비용 가정 여부를 표시한다.
- 원본 연구 SVG의 포인터 조회는 불규칙 표본의 실제 봉 좌표에서 가장 가까운 값을 선택한다. 가격 조회는 키보드 방향키/Home/End/PageUp/PageDown으로도 가능하며 매수·매도/거래 손익을 접근성 값으로 전달한다.

### 반응형과 접근성

- 320, 390, 768, 1024, 1280, 1440, 1680px에서 가로 오버플로와 텍스트 겹침이 없다.
- 키보드만으로 첫 질문부터 아티팩트 열기까지 완료할 수 있다.
- 대화 원본의 압축형 보조 버튼은 24px 이상, 모바일 주요 선택과 기존 퍼널 주요 행동은 44px 이상이다.
- 사이드바의 독립 버튼은 44px 클릭 영역을 유지하되, 문장 안 로그인 동작에는 그 최소 크기를 적용하지 않는다. 원본의 점선 밑줄·문장 기준선을 유지하고 안내 SVG는 첫 줄의 중심에 맞춘다. 접힌 레일의 위·아래 중심축과 긴 프로필 이름의 말줄임도 실제 좌표로 검사한다.
- 숫자 뒤 한글 단위는 공용 숫자 글꼴의 한글 fallback을 사용한다. 모바일·태블릿(860px 이하)의 인증·문서·연결 입력은 16px 이상으로 유지한다.
- 연구 SVG는 화면 너비와 좌표계를 함께 조정해 축 글자가 실제 11px, BUY/SELL 마커가 9px 너비로 읽히게 한다. 원본 fixture와 결과 의미는 유지한다. 낮은 화면에서는 입력 높이를 제한해 긴 초안 작성 중에도 문서 읽기 영역이 남아야 한다.
- reduced motion과 reduced transparency에서 기능 손실이 없다.
- 200% 확대에서 핵심 행동과 결과가 잘리지 않는다.

### 회귀 검증

- lint, typecheck, build, Playwright desktop/mobile 통과
- console error, unhandled rejection, duplicate id 0건
- 인증 코드 재전송 대기는 경과 시간 기준이며 탭 복귀 시 즉시 동기화한다. 공개 페이지로 뒤로가기할 때 이전 앱의 포털·inert·스크롤 잠금을 해제하되 대화 초안은 유지한다.
- 스크린샷 회귀는 홈, 자유 대화, 전략 초안, 실행 중, 결과, 실패 수정, 아티팩트 열린 상태를 포함한다.
- Mock/실연결 feature flag 조합별로 데이터 출처와 실행 가능 상태를 검증한다.

### 로컬 검수 기록

상세 버그·실패→수정→재검증 이력과 미해결 위험은 [Bugfix_report.md](Bugfix_report.md)에서 단일 관리한다. 기존 상세 기록은 보고서 §8로 누락 없이 이관했다. 이 문서는 원본 이식 대조(§18), 디자인 합격 기준(§19), 디자인 결정(§21)을 계속 소유한다. 최신 검증 및 Git 전달 상태는 보고서 §6/7을 확인한다. local in_review이며 실제 서비스 연결·운영 승격과 별도다.

## 20. 금지 패턴

- 원본의 연구 진행·문서를 반복 Mock 설명문이나 별도 중간 화면으로 대체
- 원본의 다른 artifact를 하나의 카드로 통합하거나 문서 종류·버튼·카피를 임의 축약
- 기존 Mock 질문 분기에 맞춰 클라이언트의 자유 대화·연구 흐름을 재해석
- 시장과 전략을 별도 제품처럼 분리하는 최상위 탭
- 질문 후 중간 랜딩이나 로딩 페이지로 전환
- 답변을 박스 하나로 즉시 열어 보이는 동작
- 의미 없는 생각 애니메이션과 고정된 진행 퍼센트
- 모든 콘텐츠를 glass 카드로 감싸는 구성
- 같은 화면에서 서로 다른 색·radius·타이포 체계 혼용
- 작은 영문 라벨과 지표를 과도하게 채운 대시보드
- 실제 데이터처럼 보이는 합성 가격과 체결
- 브라우저 storage에 API key, token, secret 저장
- 백테스트 결과만으로 실거래를 바로 활성화하는 버튼

## 21. Decision Log

| 날짜 | 결정 | 상태 | 근거와 영향 |
|---|---|---|---|
| 2026-09-09 | 실제 차트 계약 협업과 현 디자인의 독립 렌더러 준비 | in_review | 사용자 승인·PM5694b6f 기준. 원천 reader는 있으나 발행 chart producer가 없어 Web #25로 계약/writer 예약 요청. 현재 토큰·초 단위 체결·60초 client replay/Skip을 독립 시험하며 제품 tfw/실제 API에 임의 연결하지 않는다. 상세는 §9/BF-095~099, public/운영 권위 불변. |
| 2026-09-09 | 완료 결과의 실패/복구를 대화 맥락과 분리 | in_review | PM504a373d 기준, 원본 토큰의 inline 결과 조회/재시도. 초안/job 보존, 네트워크와401 분리, GET4개 단일 그룹·오래된 세션 응답 무시·신뢰 검증 유지. 승인된 #104 설치물의 독립8790 replay 연결 검증 후 종료하며 live8789/public4176·공용 계약/실행 권한은 불변. BF-090~092 및 미연결 경계는 Bugfix_report 참고. |
| 2026-09-09 | 내부 검수 화면의 시스템 한글 폰트 의존 제거 | in_review | 직접 캡처에서 한글 glyph 누락을 확인했다. 기존 Noto Sans KR Variable 자산을 내부 기본/fixture entry에서도 불러오고 family를 일치시킨다. Client 원본 UI/카피 변경 없음. BF-094와 desktop/mobile 캡처 증거 참고. |
| 2026-09-09 | 내부 대화의 partial Draft 복구를 새 SDK에 연결 | in_review | PM773636d §16의 Contracts #28 rc2 소비. 기존 세션은 소유권 GET 후 복구, 신규 세션은 저장 상태 초기화. 복구 중 입력/reset/logout을 잠그고 초안 복구와 원래 대화 내역 복원을 구분한다. cached Draft 우회 제거, GET으로 READY 권위를 만들지 않음. 원본 디자인·public4176·live8789 pin/자산 보존, 새 빌드는 테스트 브라우저만 소비. 사용자 승인으로 구 UI4174 listener만 종료하고 코드 보존. exact 근거와 후속 위험은 Bugfix_report의 SDK 절. |
| 2026-09-09 | 원본 완료 문서의 선택적 상세 차트 연결 | in_review | 사용자 배치/일관성 승인. 원본 대화·Activity·Critic·문서 흐름 유지, 같은 문서 질문·초안과 버전별 차트/거래를 연결한다. 현재 Client fixture에 없는 OHLC·거래량·실데이터를 합성하지 않는다. Skip은 결과 시각화만 종료. 실제 서비스 계약/내부 서버·기존 전문 작업본/운영 배포 불변. 로컬 구현·검수 범위와 한계는 §9 및 Bugfix_report의 상세 차트 절. |
| 2026-09-09 | 오류 의미와 편집 가능 상태를 원본 대화 안에서 일치 | in_review | 최신 Web/main 및 클라이언트 원본 HEAD를 재확인했다. 서버 오류를 모두 설정 오류로 표시하지 않고 고정 상태별 카피로 구분하며 세션 오류로 요청 미실행을 단정하지 않는다. 실제 소비에서 요청/승인 재개 중 메시지 수정도 컴포저와 함께 잠그되 복사는 유지한다. 원본 레이아웃/Mock 기본 동작/API 계약은 불변. PM a40ce9a·상세 증거는 Bugfix_report 후속 검수 절을 따른다. |
| 2026-09-08 | 원본 대화 UI에 내부 v0.1 세션·대화 연결 | in_review | 공개 Mock 진입 불변. 내부 #/client에서 기존 controller가 API·승인·출처를 소유하고 ClientConversation/Activity는 표시만 담당한다. 요청 대기를 허구 Critic/검증 타이머로 채우지 않는다. 원래 응답 유실과 최신 초안 GET을 구분하고 GET으로 검증 후보 권위를 만들지 않는다. 로컬 저장 실패와 서버 성공을 구분하되 승인 retry key 저장 실패에서는 다음 mutation을 막는다. 실제 HTTP 부분 초안 계약 불일치/공유 서버 동시성, 외부 모델/730일 결과/세션 재진입은 미완료. PM5f23221·Backend99/AI28 소비, 상세 시험과 위험은 Bugfix_report.md. |
| 2026-09-08 | UI·UX 디자인 작업을 최신 main과 분리 결합 | accepted | 사용자 단계별 연결 지시. Web c156d10 위 별도 feat/web/ui-service-integration worktree에 현행 Client UI를 선택 이식하고 최신 차트/서비스/보고서50파일과 기존 작업본을 보존한다. 원본 정상 디자인 불변, 모바일 저장 오류 알림의 메뉴 가림만 교정. build/lint·최종736개 중710 PASS/26 SKIP, 시각 비교 근거는 Bugfix_report.md integration-baseline. PM은9fe50bc 읽기 전용, 실API/운영/원격 인계 및 사용자 화면 승인은 후속이다. |
| 2026-09-08 | 버그 보고 단일 정본과 계정 닫기 포커스 보강 | accepted | 사용자 지속 기록·반복 검수 요청. 기존 §19 상세 QA는 Bugfix_report.md §8로 무손실 이관하고 버그/OPEN/Git 인벤토리·실행 결과를 같은 보고서에 갱신한다. 계정 배경 클릭의 기본 focus reset과 StrictMode 최초 진입점 재기억을 BF-061/062로 교정한다. 원본 정상 디자인/실제 계약 불변. 전체 회귀의 구형 추천 전환 간헐 재발도 OPEN-001로 유지하며 단독 재통과로 닫지 않는다. local in_review, 운영 승격 별도. |
| 2026-09-08 | 선택적 페이지/도움말 로드 실패 격리·조합 취소 보존 | accepted | 사용자 심층 검수 요청. 공개/도움말 청크 오류를 대화와 분리하고 로딩/실패 중 복귀·명시적 새로고침·보이는 포커스를 제공한다. 네 입력 경로의 composing Escape는 편집을 닫지 않는다. 신규18개 및 재현/교정/반복 검증은 §19. 원본 정상 UI·실제 계약 불변, local in_review이며 운영 승격 아님. |
| 2026-09-08 | 교차 화면·연속 사용 상태 전이 교정 | accepted | 사용자 심층 검수 요청. 메뉴/언어/Artifacts의 경로 이탈 해제·키보드 대상 및 반응형 포커스, 복사 최신 요청 결과, 연결 확인 중 읽기 전용 입력을 교정한다. 신규22개 회귀·3회 반복66개 통과·긴 사용자 동선 및 preview 근거는 §19. 원본 디자인·대화/연구 지속·실제 계약은 유지하며 local in_review, 운영 승격 별도. |
| 2026-09-08 | 10회 영역별 검수·연속 실패/반응형/차트 표시 교정 | accepted | 사용자10회 반복 요청. 복사 실패 성공 잔류, 회복 후 재실패 안내 누락, 검색 중 화면 축소 시 활성 열 불일치, 위임 차트 주기 설명 불일치를 재현 후 교정한다. 신규10개 회귀·10회별 증거/한계는 §19. 원본 UI·실제 API/거래 의미론 유지, local in_review 및 운영 승격 별도. |
| 2026-09-08 | 위임 가이드 공개 경로 해제·저장 실패 왕복 보존 | accepted | 사용자 반복 검수 요청. native dialog 이탈과 위임 답변 소실을 재현하고 공개 경로 해제·허용 필드 임시 보관·세션 삭제·모바일 안내 비겹침을 교정한다. 신규8개 회귀·production preview·범위 제한 agy 검수 근거는 §19. 원본 정상 UI·실제 연결 계약 불변, local in_review 및 운영 승격 별도. |
| 2026-09-08 | 연구 임시 보관·행 초안 및 설정 부분 저장 복구 | accepted | 사용자 반복 검수 요청. 저장 차단 중 동일 페이지 왕복 보존·행별 초안·세션 삭제 정리, 설정 미저장 선택 유지/재시도/닫기 조건을 교정한다. 신규8개 회귀·실패 및 수정 후 증거는 §19. 원본 UI·실제 계약 유지, 새로고침/다른 기기 메모리 보존 보장 및 운영 승격 아님. |
| 2026-09-08 | 추가 상태 경계·설정 키보드·독립 초안 보존 교정 | accepted | 사용자 계속 검수 지시. 보이는 메뉴 포커스/반응형 복귀, 독립 입력 초안, 정상 턴 분리 복원, 불일치 running 정리, 행 코멘트 outline 겹침을 교정한다. 신규16개 회귀·실패/재검증은 §19. 실제 계약·원본 문구 불변이며 local in_review다. |
| 2026-09-08 | 공개 경로 스크롤 복원과 숫자 부분 일치 제거, 반복 코드 검수 | accepted | 사용자 수정 승인. 드로어 경로 이탈/반응형 포커스, 손절·익절 전체 입력 검사/초안 보존, 대화 수치 오선택 방지를 신규18개 회귀로 검증한다. 원본 디자인·선택지·실제 거래 계약은 유지하며 §19에 세 차례 검수 증거를 기록한다. 로컬 in_review, 운영 승격 별도. |
| 2026-09-08 | 코드 리뷰 추가 3건 교정 및 독립 병렬 검수 | accepted | 사용자 수정 승인. native 입력 dialog 종료·로그인 포커스 복귀, 비유한 숫자의 인라인 오류/초안 보존, 생성 시점/활성 탭을 지키는 캐시 복원을 적용한다. 실제 계약·원본 디자인·fixture 유지, 합격 증거는 §19. 운영 승격과는 별도다. |
| 2026-09-08 | 펼친 사이드바의 계정/설정 위치 교환 | accepted | 사용자 요청. 펼침에서 계정 왼쪽/설정 오른쪽, 접힌 레일은 유지. DOM/Tab 순서와 상태별 클릭 검증. 당시 분리한 추가 3건은 후속 수정 승인으로 위 결정에 따라 교정한다. |
| 2026-09-08 | 전수검수로 확인한 6건의 동작·접근성 결함 교정 | accepted | 사용자 수정 승인. 정상 세션 분리 복원/안내, 확정 조건 기반 자동 제목, 봉 좌표 기반 포인터·키보드 조회, 실제 경과 시간 재전송 대기, 공개 경로 이동 시 포털 정리. 원본 레이아웃/fixture·사용자 지정 제목·대화 지속·실제 API/주문 계약 유지. 테스트와 한계는 §19, 운영 승격 별도. |
| 2026-09-08 | 화면 이탈과 작업 중지를 분리하고 일반 화면의 재생 조작부 제거 | accepted | 사용자 지시. 연구의 자동 pause/대화의 reload stop을 제거하고 시간 기준 복원·세션 캐시·기록 완료 갱신·위임 검증 복원을 적용. 개발 재생 도구는 DEV opt-in에만 표시. 원본 UI/fixture·명시적 중지·실제 서비스/인증 경계 유지. 검증과 잔여 범위는 §19. |
| 2026-09-08 | 주요 화면 디테일·좁은 화면 읽기 기준 보완 | accepted | 숫자 한글 fallback, Activity 아이콘, 문서 보조정보 대비, 너비 대응 SVG, 낮은 화면 본문 확보, 태블릿 입력·위임 헤더, 소개 고정 헤더 교정. 원본 카피/fixture 및 실행 계약 유지. 화면별 수정·점검·잔여 한계는 §19에서 구분. 로컬 UI 검수이며 운영 승인 아님. |
| 2026-09-08 | 원본 사이드바의 인라인 동작과 독립 버튼 레이아웃 분리 | accepted | 44px 공통 규칙으로 발생한 안내문/SVG 정렬 회귀 수정. 레일 축·긴 이름·모바일 하단 배치를 함께 검증. 독립 버튼 클릭 크기·원본 문구·SVG 유지, 10개 신규 포함 관련80개 통과. 로컬 시각 보완이며 전체 UI/운영 승인이 아님. |
| 2026-09-07 | 원본 유지 상태에서 연속 사용 결함 교정 | accepted | 사용자 추가 검수 지시. 대화·연구 읽기 위치, 120자 제목 편집, 설정/문서 focus, 태블릿 클릭 영역, 낮은 화면 설정, 필수 설명 대비와 입력창 회전을 교정. 새 서비스/주문 계약이나 카피 변경 없음. agy는 읽기 전용 두 차례 검수, 실제 실패→통과 및 전체 회귀는 §19 기록. |
| 2026-09-07 | 원본 전체 UI를 기본 진입으로 교체 | accepted | ClientMainExperience/세션·AccountUI·ResearchWorkspace·DelegationWorkspace·공개7언어를 연결. 기존 퍼널은 DEV-only 회귀, 전문 차트 worktree 보존. 조건 전달/완료 기록/삭제 캐시/모바일 겹침 교정. fixture와 실제 서비스 경계·별도 테스트 유지. 로컬만 검수 |
| 2026-09-07 | 원본 전체 60항목 대조 및 홈·언어통화 첫 복원 | accepted | 공개/Git 해시 동일. gDoc/tf 두 유효 흐름을 구분하며 기존 Mock 분기를 정본으로 삼지 않는다. 고정 입력·ko/USD 전용 제한 폐기, 원본 데이터 추출·7언어/37통화·모바일시트·저장/실패·글꼴 복원. 인증 시점은 PM 확인 필요. 실제 계약·전체 이식·운영 완료와 구분 |
| 2026-09-07 | Activity/Critic 원본 문서 복원과 개발 도구 분리 | accepted | Critic 3블록, Activity 타임라인/헤더 상태, 원본 문서 SVG·연구 계획 타이포 복원. 개발 도구의 일반 화면 노출 방식은 09-08 결정으로 대체. v1/v2 시점·판정 공급 모델과 문서 스크롤 복원 검사. 전체 문서/실제 연구/전문 차트 연결·운영 승격 완료 아님 |
| 2026-09-07 | 원본 기준 재구성 및 제품 표현과 개발 검수의 분리 | accepted | 사용자 확정. PM §16/README/WORK_LEDGER에 인계. 원본 UI를 먼저 계승하고 실제 adapter와 보존된 전문 차트를 연결한다. 반복 Mock 본문 설명과 Activity finding을 Critic 전체로 대체하는 방식을 중단한다. 단계별 구현 상태는 §18에서 추적하며 원본 전체 이식·실제 연결·배포는 미완료 |
| 2026-09-07 | 연구 시작·g-doc·Critic 발견의 부분 이식 | superseded | 같은 셸과 Activity 5행 설명은 구현됐지만 별도 Critic 원문을 복제 카드로 대체했다. 반복 Mock 본문 설명과 부분 이식을 제품 기준으로 삼지 않고, 원본 기준 재구성 결정으로 대체한다. 이벤트 없는 실제 결과 생성 금지와 기존 작업 보존은 유지한다. |
| 2026-09-07 | 리서치 메뉴·연결 화면·원본 SVG 이식 | accepted | `ClientChrome`, `ClientIcon`, `ClientResearchHub`, `research-library.ts`가 원본 메뉴와 로컬 연구 복원을 담당한다. 사용자 카피를 유지하고 가독성·모바일 그리드·포커스·저장 실패 가림을 보완한다. 랭킹은 원본 Mock fixture, 공유는 탭 내 미리보기, 예약은 미연결 빈 상태다. `research-navigation.spec.ts`로 새 연구/복원·이동·SVG·외부 요청 경계를 검증하며 실제 API·원본 내 트레이딩/Live·운영 승격 완료로 간주하지 않는다. |
| 2026-09-07 | 클라이언트 이식 범위를 리서치를 포함한 대화 전체로 명확화 | accepted | `acccc7f`의 활동 타임라인·경과 시간·작업 요약·실패/수정/재검증·문서 맥락을 이식 추적 표로 관리한다. `ClientResearchActivity` 표시 구조만 우선 연결하며 실제 대화·연구 이벤트·전문 차트 재통합은 미완료로 명시한다. 고정 대기/검증 대본을 실제 연구로 표시하지 않고 승인된 계약을 소비한다. 운영 승격은 하지 않는다. |
| 2026-09-07 | 전체 화면의 한국어 가독성과 반응형 합격 기준 보강 | accepted | 클라이언트 브랜드·카피를 유지하며 어절 줄바꿈, 긴 문자열 탈출, 금액/단위, 문서·해설의 읽기 폭을 정리한다. 작은 높이와 사용자 지정 글자 간격에서 고정 요소가 본문을 가리면 인플로우로 전환한다. 모바일 제목 편집·입력 너비 변경·연결 오류를 추가 회귀 검증한다. 운영 승격과 실제 거래 Gate는 바뀌지 않는다. |
| 2026-09-07 | 클라이언트 정보·다운로드·정책 화면을 내부 React 경로로 이식 | accepted | Git에는 원본 페이지와 자산이 존재했으며 외부 링크 처리가 이식 누락 원인이었다. `ClientPublicPages`·`SiteRouter`·`InternalLink`가 페이지와 경로를 소유한다. FAQ·요금제 비교·3개 앱 화면 캐러셀·5개 정책 탭·목차·도움말을 로컬로 제공한다. 임시 스토어·QR·고객지원 주소는 실행하지 않고 실제 연결과 카피 초안을 구분한다. 다른 원본 기능은 안내 화면 대조 표의 별도 검증 대상으로 남긴다. |
| 2026-09-07 | 최신 `acccc7f`의 대화 구조를 `ClientConversation`으로 이식 | accepted | 44px 헤더, 820px 본문·컴포저, 16px 메시지 간격, 아바타 없는 응답, 대칭 18px 사용자 버블, 10px 입력창 radius, 300px Artifacts 목록을 사용한다. 기존 임의 시그널 카드·완성률·고정 플로팅 카드를 제거한다. 한글 IME·초안 유지·중지·문서 탭·모바일 검수를 요구한다. 소스의 손상된 인코딩 문자열과 데모 인증·프록시는 이식하지 않는다. |
| 2026-09-07 | 입력창·셸 상호작용을 React 컴포넌트로 분리하고 후속 Mock 화면에도 클라이언트 색상·폰트를 공통 적용 | accepted | 한 줄 스크롤 결함, 숨은 버튼의 키보드 포커스, 사이드바 개폐, 화면 간 밝은 테마 회귀를 검증한다. 클라이언트 카피와 제품 계약은 유지한다. |
| 2026-09-06 | 배포된 `tesia-lab`을 로고·파비콘·첫 화면·카피·인터랙션의 시각 정본으로 승격 | accepted | 기존의 “경험 원형만 참고” 해석을 폐기한다. React 구조와 제품 계약만 현 프로젝트 방식으로 유지하고, 보이는 결과는 클라이언트 배포본과 일치시킨다. |
| 2026-09-06 | `tesia-lab`의 대화 중심 UI·UX를 TETH 웹의 경험 원형으로 사용 | superseded | 같은 날 시각 정본으로 채택한 결정이 대체한다. |
| 2026-09-06 | 시장 질문과 전략 생성을 하나의 연속 대화로 통합 | accepted | 별도 탭과 페이지 전환에서 발생하던 맥락 손실을 제거한다. |
| 2026-09-06 | 차트·전략·보고서를 대화의 아티팩트로 배치 | accepted | 대화를 명령 센터로 유지하면서 전문 분석 공간을 확보한다. |
| 2026-09-06 | 랩의 합성 엔진, Mock 인증, 터널 프록시는 이식하지 않음 | accepted | 실제 계약과 보안 경계를 보존하고 UI 원형과 실행 의미론을 분리한다. |

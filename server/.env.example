# 복사해서 같은 폴더에 .env 로 저장한 뒤 값을 채우세요. (.env는 git에 올라가지 않습니다)
# console.anthropic.com 에서 발급한 API 키. 비워두면 `ant auth login` 프로필을 자동 사용합니다.
ANTHROPIC_API_KEY=

# 선택: 모델/포트 재정의
# TETH_AI_MODEL=claude-opus-5
# TETH_AI_PORT=8799

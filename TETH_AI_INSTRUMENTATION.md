# TETH AI 장기 응답 원인 규명 — 계측 요구 리포트

작성: 2026-09-10, say/work 개편 PR3. 대상: AI 프록시(server/index.mjs · worker.mjs)와 그 상류.

## 배경

tool(웹 검색·페이지 열기·코드 실행)이 켜져 있던 구성에서 단일 응답이 수백 초(관측 최대 614초)까지
길어지는 사례가 있었다. 현재 1단계(lite, tool 미연결)에서는 ~40초 수준으로 해소됐지만, 3단계에서
tool 을 다시 켜면 같은 문제가 돌아온다. 프록시에는 호출 단위 구조화 로그가 없어 "어느 구간이
느린가"를 사후에 답할 수 없다 — 원인 조사 대신, 조사를 가능하게 만들 계측 목록을 요구한다.

## 요구 메트릭 (프록시 응답 1건 = 1 레코드)

| 항목 | 형태 | 왜 필요한가 |
|---|---|---|
| 요청 수신~첫 SSE 이벤트(TTFB) | ms | thinking 선행 지연과 네트워크 지연 분리 |
| 요청 수신~첫 text 델타 | ms | 사용자 체감 "첫 글자" 지연의 서버 몫 확정 |
| thinking 구간 총 시간·토큰 | ms, tokens | effort 설정 대비 사고 비용 검증 |
| tool 호출별: 종류, 시작~결과 수신 시간 | 배열 | 개별 검색/페이지 열기 중 어떤 호출이 병목인지 |
| tool 호출 횟수(종류별) | count | "같은 검색 반복" 류 낭비 탐지 — 합칠 수 있는 계산 식별 |
| 상류 API 재시도 횟수·사유 | count, code | 529/overloaded 재시도가 총시간에 숨는 것 방지 |
| 총 출력 토큰·usage | tokens | 길이 기인 지연과 대기 기인 지연 분리 |
| 종료 사유 (done/error/refusal/abort) | enum | 실패 모드별 소요시간 분포 |

## 산출 형태 제안

- 프록시가 응답 종료 시 위 필드를 JSON 1줄로 콘솔(로컬)/Workers 로그(배포)에 기록.
- 클라이언트는 이미 ack 첫 델타 레이턴시를 기록 중(`window.__tethAckStats`, dev) —
  서버 레코드와 requestId 로 짝지을 수 있게 프록시가 응답 헤더에 요청 ID를 에코.

## 판단 기준 (계측 후)

- tool 호출별 합계가 총시간의 대부분이면: 호출 상한/병렬화/캐시 검토.
- 동일 쿼리 반복이 확인되면: 프록시 단 검색 결과 단기 캐시.
- thinking 구간이 지배적이면: effort 하향 또는 질문 유형별 effort 라우팅.

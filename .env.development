# vite dev 전용. 로컬 AI 프록시(main 브랜치 server/index.mjs, port 8799)가 떠 있으면
# 대화가 실제 Claude(claude-fable-5)로 연결된다. 프록시가 없으면 자동으로 기존
# 스크립트 응답으로 폴백한다. 프로덕션 빌드와 E2E(VITE_E2E_FAST)에는 적용되지 않는다.
VITE_TETH_AI_PROXY=http://localhost:8799

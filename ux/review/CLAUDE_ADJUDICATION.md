# AGY 최종 비판 판정 (Claude, 제품 책임자) — Codex 수정 라운드 지시

## 수정한다 (Codex가 구현)
| AGY # | 판정 | 구현 지시 |
|---|---|---|
| 01 | 수용, **완료**(커밋 f7b78d4) | 청구 라인에 "- 무료 혜택 ₩1,000" 항 추가됨. 이용 현황 04 화면에서 양변 일치 확인만 할 것 |
| 02 | 수용 P0 | 미활성 뷰(tfTradeInactiveView)의 활성화 카드는 `derive.activation.api==='none' && derive.activation.card==='none'`일 때만 렌더. 하나라도 완료면 카드 대신 한 줄 상태("Bitget 연결됨" 또는 "카드 등록됨")만 |
| 03 | 수용 P0 | 미활성 뷰 1차 행은 `derive.primaryCTA`를 우선한다: primaryCTA.action이 활성화류(tfAcSheet)이면 그것을 라임 1차 행으로 맨 위에, 그 아래 런처 3행(모의로 켜보기/직접 만들기/골라보기)은 보조(비라임). 그 외 상태는 현행대로 |
| 04 | 수용 P0 | 거래소 연결 페이지(tfBrokersView) 연결됨 상태: "약 1분/자산 이동 없음" 불릿을 "연결됨 (요금 연동 ✓, 실행 연결 ✓)" 상태 행으로 교체. 1차 CTA는 `derive.facts.owned>0 ? '터미널 열기' : '첫 전략 실행하기'`(둘 다 tfNav('#/trade')) |
| 05 | 수용 P0 | 터미널 시뮬 가격 표기 통일: 상태 블록 "마지막 봉", 규칙 카드의 손절/익절/주문 가격, 질문 칩 응답의 가격은 `tfTmPx(n)` = `n.toLocaleString()+' USDT'`로 (₩ 금지). 운용금·P&L(원화 예산 기준)은 ₩ 유지 |
| 06 | 수용 P0 | `#/share/copy/<nick>` 딥링크에서 mkFollowSheet가 실제로 열리도록(현재 상세만 렌더). 렌더 순서 문제면 상세 렌더 후 `setTimeout(0)`으로 시트 오픈 |
| 08 | 수용 P1 | 온보딩 바 현재 단계 = 첫 미완료 단계 |
| 10 | 수용 P1 | 마켓 카드에서 "팔로워 수익 -" 컬럼 삭제(데이터 있을 때만 조건부) |
| 11 | 수용 P1 | 미활성 뷰의 인라인 활성화 카드 버튼은 모달 시트가 아니라 연결 폼(view 'conn')으로 직행: `tfAcSheetView('bitget',{direct:true})` 같은 옵션. 카드 버튼도 카드 폼 직행 |
| 13 | 수용 P1 | 모바일(<768) 마켓 상단: 탭 1행 + 정렬/기간/필터를 가로 스크롤 칩 1행으로 압축(검색은 접힘 아이콘). 첫 카드가 첫 화면에 보이게 |
| 16 | 수용 P1 | 미활성 뷰 하단 "터미널 미리 보기" 링크 삭제 |
| 17 | 수용 P1 | 모바일 터미널 헤더 아래 요약 1줄(운용 ₩, 누적 P&L) 추가, 긴급 정지 버튼 우측 여백 14px |
| 20 | 수용 P2(문구) | 거래소 상세 "카피 트레이딩" → "따라가기" |
| 불일치 #6 | 수용 | 마켓·상세 기본 기간을 '전체'로(랜딩 카드의 +23.9%와 같은 기준). 배지 "검증 기준(백테스트 42개월)" |
| 15 | 검증 후 판단 | "전략이 시작됐어요" 토스트가 3초 내 자동 소멸하는지 CDP로 확인. 소멸하면 조치 없음, 아니면 toast 수명 2.5초로 |

## 기각한다 (근거)
- 07: 에이전트 fixture 카드는 QA 오버레이(`판단 로그 채움`)에서만 나오며 일반 사용자 경로에 없다. 라벨을 "QA 예시, 시뮬레이션"으로 바꾸는 것만 수용.
- 09(가운뎃점): 발주자 문장부호 규칙이 가운뎃점을 금지한다. 쉼표 유지. 제목 중복은 커밋 54a5f10에서 이미 차별화됨(태블릿 스크린샷이 그 증거).
- 12: AI 판단 카드에 "30일 +18.4%" 같은 수치를 넣는 것은 엔진에 없는 수치의 발명(카피 규칙 2, D26). 기각.
- 18: `#/share/s/세븐틴층/1y/perf` 진입 시 성과 탭이 정상 활성됨을 CDP로 확인(크롤 스크립트의 호출 오류였음). 기각.
- 19: 데모 6개 이름은 preview 전용 데모 데이터. P2, 이번 라운드 범위 밖. QA 버튼 겹침은 개발 환경 전용.

## Codex 추가 임무
- 위 수정 후 11 프리셋 × (#/trade, #/plan, 거래소 연결, 마켓) 재검증, 예외 0, 390px 가로 스크롤 0.
- 회귀 검수 항목(codex-fix.md)은 그대로.
